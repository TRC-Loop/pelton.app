---
import Icon from './Icon.astro'
import AnchorHeading from './AnchorHeading.astro'
import { mcpPrompts, mcpTools, mcpWriteTools, mcpGuarantees, repo } from '../data/site.js'

const config = `{
  "mcpServers": {
    "pelton": {
      "url": "http://127.0.0.1:8765",
      "headers": {
        "Authorization": "Bearer <your-token>"
      }
    }
  }
}`
---

<section id="mcp" class="mcp-section">
  <div class="wrap">
    <div class="section-head mcp-head">
      <AnchorHeading id="mcp">Ask your agent about your inbox</AnchorHeading>
      <p>
        Pelton speaks the Model Context Protocol, so an AI agent you already use can work with your
        cached mail. On your machine, off until you turn it on, and read-only until you grant each
        action yourself.
      </p>
    </div>

    <div class="mcp-top">
      <div class="mcp-chat">
        <div class="mcp-turn">
          <span class="mcp-who">You</span>
          <p>Which invoices arrived this month, and what do they add up to?</p>
        </div>

        <div class="mcp-calls">
          <div class="mcp-call">
            <Icon name="plug-connected" size={13} stroke={1.9} />
            <code>search_messages</code>
            <span class="mcp-args">"invoice", after: 30d</span>
            <span class="mcp-ok"><Icon name="check" size={13} stroke={2.2} /></span>
          </div>
          <div class="mcp-call">
            <Icon name="plug-connected" size={13} stroke={1.9} />
            <code>get_message</code>
            <span class="mcp-args">4 messages</span>
            <span class="mcp-ok"><Icon name="check" size={13} stroke={2.2} /></span>
          </div>
        </div>

        <div class="mcp-turn is-agent">
          <span class="mcp-who">Agent</span>
          <p>
            Four invoices this month totalling <strong>€1,284.40</strong>, from Stripe, AWS, Namecheap
            and Fastmail. The AWS one is the only one still unpaid.
          </p>
        </div>
        <p class="mcp-illus">* Illustrative. Not a real exchange.</p>
      </div>

      <div class="mcp-prompts">
        <AnchorHeading id="mcp-prompts" tag="h3">Things worth asking</AnchorHeading>
        <ul>
          {mcpPrompts.map((p) => (
            <li>{p}</li>
          ))}
        </ul>
        <p class="mcp-prompts-note">
          The last one still ends with you. Even with sending switched on, an agent can only queue a
          message in Pelton for you to read and approve.
        </p>
      </div>
    </div>

    <div class="mcp-tools">
      <div class="mcp-sub">
        <AnchorHeading id="mcp-tools" tag="h3">Five tools that read, seven that act</AnchorHeading>
        <p>
          The whole surface an agent gets. The read tools work as soon as the server is on. Each
          write tool is a switch of its own, and every one of them starts off.
        </p>
      </div>

      <span class="mcp-tool-group">Read, available once enabled</span>
      <dl class="mcp-tool-list">
        {mcpTools.map((t) => (
          <div class="mcp-tool">
            <dt><code>{t.name}</code></dt>
            <dd>{t.desc}</dd>
          </div>
        ))}
      </dl>

      <span class="mcp-tool-group">Write, off until you grant each one</span>
      <dl class="mcp-tool-list">
        {mcpWriteTools.map((t) => (
          <div class="mcp-tool is-write">
            <dt><code>{t.name}</code></dt>
            <dd>{t.desc}</dd>
          </div>
        ))}
      </dl>
    </div>

    <div class="mcp-bottom">
      <div class="mcp-setup">
        <div class="mcp-sub">
          <AnchorHeading id="mcp-setup" tag="h3">Connecting takes one paste</AnchorHeading>
          <p>
            Enable the server, copy the token, and point your client at it. Pelton shows you this exact
            snippet with your own token already in it.
          </p>
        </div>
        <div class="mcp-snippet">
          <div class="mcp-snippet-head">
            <span>MCP client configuration</span>
            <span class="mcp-snippet-lang">JSON</span>
          </div>
          <pre>{config}</pre>
        </div>
        <a class="mcp-docs" href={repo.docs} target="_blank" rel="noreferrer noopener">
          <span>Setup guide in the docs</span>
          <Icon name="external-link" size={14} />
        </a>
      </div>

      <div class="mcp-guarantees">
        <div class="mcp-sub">
          <AnchorHeading id="mcp-limits" tag="h3">Where the limits are</AnchorHeading>
          <p>Handing an agent your mail is a real decision. Here is exactly what you are agreeing to.</p>
        </div>
        <div class="mcp-g-list">
          {mcpGuarantees.map((g) => (
            <div class="mcp-g">
              <span class="mcp-g-icon"><Icon name={g.icon} size={17} stroke={1.8} /></span>
              <div>
                <h4>{g.title}</h4>
                <p>{g.body}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
